(* TEST
  include refutable;
*)

(* error is that input list xs should be nonempty *)

let rec foldl1 (type a) (f : a -> a -> a) (xs : list a) : a =
  match xs with
  | z :: [] -> z
  | z :: w :: tl -> foldl1 a f ((f z w) :: tl)
  end
  (* ERROR: `xs` could be empty. We are missing a match case *)
