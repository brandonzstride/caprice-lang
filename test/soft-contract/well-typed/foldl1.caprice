(* TEST
  include exhaustible;
*)

let is_nonempty ls =
  match ls with
  | [] -> false
  | _ -> true
  end

let rec foldl1 (type a) (f : a -> a -> a) (xs : list a | is_nonempty xs) : a =
  match xs with
  | z :: [] -> z
  | z :: w :: tl -> foldl1 a f ((f z w) :: tl)
  end
