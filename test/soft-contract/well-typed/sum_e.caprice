(* TEST
  typing = no-error;
  flags += " -t 2.0";
  speed = slow;
  typecheck;
*)

let rec sum x =
  if x <= 0
  then 0
  else x + sum (x - 1)

let main (n : int | n >= 14) : { i : int | 100 <= i } =
  sum n
