(* TEST
  include splayable;
*)

let is_nonempty ls =
  match ls with
  | [] -> false
  | _ -> true
  end

let rec append (type a_) (a : list a_) (b : list a_ | is_nonempty b) : { l : list a_ | is_nonempty l } =
  match a with
  | [] -> b
  | hd :: tl -> hd :: append a_ tl b
  end
