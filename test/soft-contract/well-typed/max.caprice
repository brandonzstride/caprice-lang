(* TEST
  include finite-well-typed;
*)

let max max2 x y z =
  max2 y z

let f x y =
  if x >= y
  then x
  else y

let main (dependent x : int) (y : int) (z : int) : { c : int | c == f x c } =
  max f y x z 
