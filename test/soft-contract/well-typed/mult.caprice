(* TEST
  include splayable;
*)

let rec mult (dep n : int | n > 0) (dep m : int | m >= 0) : { i : int | i == n * m } =
  if n <= 0 || m <= 0
  then 0
  else n + mult n (m - 1)

let sqr (dependent n : int | n > 1) : { c : int | c >= n + 1 } =
  mult n n