(* TEST
  include splayable;
*)

let rec mult (dep x : int | x > 0) (dep y : int | y >= 0) : { z : int | z == x * y } =
  if x <= 0 || y <= 0
  then 0
  else x + mult x (y - 1)

let main (n : int | n >= 6) : { i : int | 600 <= i } = 
  mult 100 n
