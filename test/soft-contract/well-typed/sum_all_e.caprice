(* TEST
  include splayable;
*)

let rec sum (dep x : int | x >= 0) : { y : int | y == x * (x + 1) / 2 } =
  if x <= 0
  then 0
  else x + sum (x - 1)

let h y =
  let b = y + y <= sum y
  in
  b

let main (n : int | n > 2) : { b : bool | b } =
  h n
