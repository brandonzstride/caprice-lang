(* TEST
  include finite-well-typed;
*)


(*
  This test is taken from OCaml:
    testsuite/tests/typing-poly/poly.ml
*)

let t = { id : (type a) -> a -> a }

let id _ x = x

let _ : t = id t { id = id }
