(* TEST
  include splayable;
*)

let has_length_two xs =
  match xs with
  | _ :: _ :: [] -> true
  | _ -> false
  end

let get_both (xs : list int | has_length_two xs) : int * int =
  match xs with
  | a :: b :: [] -> (a, b)
  end
