(* TEST
  include finite-well-typed;
*)

let is_positive_pair p = 
  match p with
  | (x, y) -> x > 0 && y > 0
  end

let multiply (p : (int * int) | is_positive_pair p) :  { i : int | i > 0 } = 
  match p with
  | a, b -> a * b
  end
