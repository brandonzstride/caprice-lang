(* TEST
  include finitely-exhaustible;
*)


let _ =
  let x = input in
  if x > 0
  then
    let _ : { a : int ; b : bool } = { a = x ; b = true } in
    ()
  else
    let _ : { a : int ; b : bool } = { a = x ; b = true } in
    ()
