
let f (a : type) (b : type) : unit =
  let rec map (f : a -> b) (ls : list a) : list b =
    match ls with
    | [] -> []
    | hd :: tl -> f hd :: tl (* ERROR: forgot to map tail *)
    end
  in
  ()
